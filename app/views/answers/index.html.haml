%h1 Listing answers
%table
  %thead
    %tr
      %th Question
      %th Text
      %th
      %th
      %th
  %tbody
    - @answers.each do |answer|
      %tr
        %td= answer.question_id
        %td= answer.text

= link_to 'New Answer', new_answer_path
